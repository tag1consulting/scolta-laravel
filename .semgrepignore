# Providing this file replaces semgrep's built-in ignore list, but semgrep
# still respects .gitignore (so vendor/ stays excluded).
.git/

# Semgrep's own default ignore list excludes tests: security rules assume
# attacker-reachable input, and test code deleting its own fixtures
# (unlink-use etc.) is noise, not signal.
tests/

# TEMPORARY: the workflows carry 19 pre-existing findings (unpinned action
# tags, a run-shell-injection, an env-secret pattern) that predate semgrep
# becoming a blocking check. Fix them and delete this entry.
.github/workflows/
