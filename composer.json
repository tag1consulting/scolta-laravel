{
    "name": "tag1/scolta-laravel",
    "version": "1.0.3-dev",
    "description": "Scolta AI Search for Laravel \u2014 zero-infrastructure AI-powered search with Pagefind",
    "type": "library",
    "license": "MIT",
    "keywords": ["laravel", "search", "pagefind", "ai", "scolta"],
    "authors": [
        {
            "name": "Tag1 Consulting",
            "homepage": "https://www.tag1.com"
        }
    ],
    "support": {
        "issues": "https://github.com/tag1consulting/scolta-laravel/issues",
        "source": "https://github.com/tag1consulting/scolta-laravel"
    },
    "require": {
        "php": ">=8.1",
        "illuminate/console": "^11.0|^12.0|^13.0",
        "illuminate/database": "^11.0|^12.0|^13.0",
        "illuminate/routing": "^11.0|^12.0|^13.0",
        "illuminate/support": "^11.0|^12.0|^13.0",
        "tag1/scolta-php": "^1.0"
    },
    "require-dev": {
        "larastan/larastan": "^3.0",
        "laravel/pint": "^1.0",
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "phpunit/phpunit": "^10.0|^11.0"
    },
    "scripts": {
        "test": "phpunit",
        "lint": "pint --test",
        "format": "pint",
        "analyse": "phpstan analyse --no-progress --memory-limit=256M",
        "validate-release": "php scripts/validate-release.php"
    },
    "autoload": {
        "psr-4": {
            "Tag1\\ScoltaLaravel\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tag1\\ScoltaLaravel\\Tests\\": "tests/"
        }
    },
    "extra": {
        "branch-alias": {
            "dev-main": "1.0.x-dev"
        },
        "laravel": {
            "providers": [
                "Tag1\\ScoltaLaravel\\ScoltaServiceProvider"
            ]
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "repositories": [
        {
            "_comment": "Development only — local path to scolta-php for symlink installs. Consumers resolve from Packagist.",
            "type": "path",
            "url": "../scolta-php"
        }
    ],
    "config": {
        "sort-packages": true,
        "audit": {
            "ignore": {
                "PKSA-mdq4-51ck-6kdq": {
                    "apply": "block",
                    "reason": "[PERMANENT:laravel-11-eol] CVE-2026-48019 (CRLF injection in default email rule) affects all laravel/framework 11.x and was reported 2026-05-19, after Laravel 11 reached EOL on 2026-03-12, so no 11.x fix will ship. Only reached via the EOL Laravel 11 CI matrix row; supported 12.x/13.x are already patched (12.60.0 / 13.10.0). apply:block stops the resolver blocking while composer audit still reports it. Remove when Laravel 11 is dropped from the test matrix."
                },
                "PKSA-8qx3-n5y5-vvnd": {
                    "apply": "block",
                    "reason": "[PERMANENT:laravel-11-eol] CVE-2025-27515 (file validation bypass) is patched in 11.44.1 / 12.1.1; only pre-patch 11.x is affected and CI installs the latest (patched) 11.x. Enumerated by the resolver across the Laravel 11 candidate range. apply:block stops the resolver blocking while composer audit still reports it. Remove when Laravel 11 is dropped from the test matrix."
                },
                "PKSA-w7xr-vk7n-rstm": {
                    "apply": "block",
                    "reason": "[PERMANENT:laravel-11-eol] CVE-2024-52301 (environment manipulation via query string) is patched in 11.31.0; only pre-patch 11.x is affected and CI installs the latest (patched) 11.x. Enumerated across the Laravel 11 candidate range. apply:block stops the resolver blocking while composer audit still reports it. Remove when Laravel 11 is dropped from the test matrix."
                },
                "PKSA-q46n-4fdk-zjr4": {
                    "apply": "block",
                    "reason": "[PERMANENT:laravel-11-eol] CVE-2024-13919 (debug-mode-only reflected XSS via route parameter) is patched in 11.36.0; only pre-patch 11.x is affected and CI installs the latest (patched) 11.x. Enumerated across the Laravel 11 candidate range. apply:block stops the resolver blocking while composer audit still reports it. Remove when Laravel 11 is dropped from the test matrix."
                },
                "PKSA-qzrn-rnz3-85w1": {
                    "apply": "block",
                    "reason": "[PERMANENT:laravel-11-eol] CVE-2024-13918 (debug-mode-only reflected XSS via request parameter) is patched in 11.36.0; only pre-patch 11.x is affected and CI installs the latest (patched) 11.x. Enumerated across the Laravel 11 candidate range. apply:block stops the resolver blocking while composer audit still reports it. Remove when Laravel 11 is dropped from the test matrix."
                },
                "PKSA-5jz8-6tcw-pbk4": {
                    "apply": "block",
                    "reason": "[DEV-ONLY:phpunit] CVE-2026-41570 (argument injection via newline in PHP INI values forwarded to child processes) in phpunit/phpunit, a dev-only dependency that is never shipped; patched in 12.5.22 / 13.1.6. apply:block stops the resolver blocking while composer audit still reports it."
                },
                "PKSA-z3gr-8qht-p93v": {
                    "apply": "block",
                    "reason": "[DEV-ONLY:phpunit] CVE-2026-24765 (unsafe deserialization in PHPT code-coverage handling) in phpunit/phpunit, a dev-only dependency that is never shipped (test-time path only). apply:block stops the resolver blocking while composer audit still reports it."
                }
            }
        }
    }
}
